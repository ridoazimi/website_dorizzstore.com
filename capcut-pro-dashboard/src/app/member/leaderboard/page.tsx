"use client";import {useEffect,useState} from "react";import Link from "next/link";export default function Page(){const[d,setD]=useState<any>(null);useEffect(()=>{fetch('/api/member/leaderboard').then(r=>r.ok?r.json():null).then(setD)},[]);return <main className="min-h-screen bg-[var(--bg-primary)] text-[var(--text-primary)] p-6 md:p-12"><div className="max-w-3xl mx-auto"><Link href="/member" className="underline">← Dashboard Member</Link><h1 className="text-3xl font-bold mt-6">Leaderboard Bulanan</h1><p className="text-[var(--text-muted)] mb-6">Top 10 dihitung dari poin referral bulan berjalan. Hadiah leaderboard tidak memotong poin.</p>{d?.top10?.map((x:any)=><div key={x.rank} className="flex justify-between p-4 border-b border-[var(--border-color)]"><span>#{x.rank} {x.name}</span><b>{x.points} poin</b></div>)}{d?.self&&<div className="mt-8 p-5 rounded-2xl border border-[var(--accent-primary)]"><b>Peringkat kamu: #{d.self.rank}</b><p>{d.self.points} poin bulan ini</p></div>}</div></main>}
