import {NextResponse} from "next/server";import {prisma} from "@/lib/db";import {requireAuth} from "@/lib/auth";
export async function GET(){const a=await requireAuth();if("error" in a)return a.error;const campaigns=await prisma.$queryRawUnsafe<any[]>(`SELECT * FROM member_leaderboard_campaigns ORDER BY month_start DESC`);const prizes=await prisma.$queryRawUnsafe<any[]>(`SELECT * FROM member_leaderboard_prizes ORDER BY campaign_id,rank`);return NextResponse.json({campaigns,prizes});}
export async function POST(req:Request){const a=await requireAuth();if("error" in a)return a.error;const {name,monthStart,monthEnd,prizes=[]}=await req.json();const rows=await prisma.$queryRawUnsafe<any[]>(`INSERT INTO member_leaderboard_campaigns(name,month_start,month_end) VALUES($1,$2::date,$3::date) RETURNING id`,name,monthStart,monthEnd);for(const p of prizes)await prisma.$executeRawUnsafe(`INSERT INTO member_leaderboard_prizes(campaign_id,rank,prize_name,notes) VALUES($1::uuid,$2,$3,$4)`,rows[0].id,Number(p.rank),p.prizeName,p.notes||null);await prisma.$executeRawUnsafe(`INSERT INTO member_admin_activity_log(admin_id,action,entity_type,entity_id,details) VALUES($1::uuid,'leaderboard_campaign_created','leaderboard_campaign',$2::uuid,$3::jsonb)`,a.user.id,rows[0].id,JSON.stringify({name,monthStart,monthEnd,prizes}));return NextResponse.json({success:true,id:rows[0].id});}
