import { NextResponse } from "next/server";import { prisma } from "@/lib/db";import { getMember } from "@/lib/member";
export async function GET(){const m=await getMember();if(!m)return NextResponse.json({error:"Unauthorized"},{status:401});const ranking=await prisma.$queryRawUnsafe<any[]>(`WITH scores AS (SELECT m.id,m.name,COALESCE(SUM(CASE WHEN l.source_type='referral_reward' AND l.points>0 THEN l.points ELSE 0 END),0)::int points FROM members m LEFT JOIN member_point_ledger l ON l.member_id=m.id AND l.created_at>=date_trunc('month',now()) AND l.created_at<date_trunc('month',now())+interval '1 month' WHERE m.status='active' GROUP BY m.id), ranked AS (SELECT *,RANK() OVER(ORDER BY points DESC,name ASC)::int rank FROM scores) SELECT * FROM ranked ORDER BY rank,name`);const self=ranking.find(r=>r.id===m.id)||null;return NextResponse.json({period:"monthly",top10:ranking.slice(0,10).map(r=>({rank:r.rank,name:r.name,points:r.points})),self:self?{rank:self.rank,name:self.name,points:self.points}:null});}
